<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "NotJustPesto" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="NotJustPesto">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# PWA manifest for installable app and offline access %>
    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <%= favicon_link_tag "icon.png" %>

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body id="body">
    <div data-controller="mobile-nav" class="h-screen w-full bg-surface-base dark:bg-gray-950 grain-texture flex flex-col md:flex-row overflow-hidden">
      <%= render "layouts/mobile_sidebar_drawer" %>
      <%= render "layouts/sidebar" %>

      <main class="flex-1 flex flex-col min-w-0 h-full bg-surface-base dark:bg-gray-950">
        <%= yield %>
      </main>
    </div>

    <%# Prefetch all recipe URLs for offline access (PWA) %>
    <% if authenticated? %>
      <div data-controller="prefetch"
           data-prefetch-urls-value="<%= Current.user.recipes.pluck(:id).map { |id| recipe_path(id) }.to_json %>"
           class="hidden"></div>
    <% end %>

    <%# Invalidate specific recipe cache after create/update/destroy %>
    <% if flash[:invalidate_cache].present? %>
      <div data-controller="cache-invalidate"
           data-cache-invalidate-url-value="<%= flash[:invalidate_cache] %>"
           class="hidden"></div>
    <% end %>
  </body>
</html>
